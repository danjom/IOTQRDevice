#ifndef OPTIONS_H
#define OPTIONS_H

enum options {MENU, SCAN, SELECT,};
enum uiblock {QRCODE, SYMBOL, DIGITS};
enum nextion {OPTIONS, PAYMENT, QRIMAGE, SUCCESS, REFUSED, WARNING};
enum signals {SETUP, READY, WRONG, VALID, CHECK, ERROR};

#endif